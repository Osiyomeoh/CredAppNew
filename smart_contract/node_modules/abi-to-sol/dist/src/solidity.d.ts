import type * as Abi from "@truffle/abi-utils";
import type { Abi as SchemaAbi } from "@truffle/contract-schema/spec";
export interface GenerateSolidityOptions {
    abi: Abi.Abi | SchemaAbi;
    name?: string;
    solidityVersion?: string;
    license?: string;
    outputAttribution?: boolean;
    outputSource?: boolean;
    prettifyOutput?: boolean;
}
export declare const generateSolidity: ({ abi, name, solidityVersion, license, outputAttribution, outputSource, prettifyOutput, }: GenerateSolidityOptions) => string;
//# sourceMappingURL=solidity.d.ts.map