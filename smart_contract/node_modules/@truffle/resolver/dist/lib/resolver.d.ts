declare const contract: any;
import type { ResolverSource, ResolvedSource } from "./source";
export interface ResolverOptions {
    includeTruffleSources?: boolean;
}
export declare class Resolver {
    options: any;
    sources: ResolverSource[];
    constructor(options: any, resolverOptions?: ResolverOptions);
    require(import_path: string, search_path?: string): ReturnType<typeof contract>;
    resolve(importPath: string, importedFrom?: string, options?: {
        compiler?: {
            name: string;
            version: string;
        };
    }): Promise<ResolvedSource>;
}
export {};
