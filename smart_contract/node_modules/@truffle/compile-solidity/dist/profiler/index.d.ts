import type Config from "@truffle/config";
export declare const Profiler: {
    updated: (options: Config) => Promise<string[]>;
    requiredSources: (options: Config) => Promise<import("@truffle/profiler/dist/src/requiredSources").RequiredSources>;
    requiredSourcesForSingleFile: (options: Config) => Promise<import("@truffle/profiler/dist/src/requiredSources").RequiredSources>;
};
